[package]
name = "stdint_intn"
version = "0.1.0"
edition = "2021"


[lib]
crate-type = ["staticlib", "lib"] # Is it because this isn't staticlib

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.131"