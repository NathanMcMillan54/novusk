[package]
name = "grubb"
version = "0.1.0"
edition = "2018"
description = "Sets up x86_64 kernel that was booted with Grub"

[dependencies]
multiboot2 = "0.12.2"
printk = { path = "../../../kernel/printk/" }
