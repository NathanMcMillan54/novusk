[package]
name = "novusk"
version = "2.1.3"
authors = ["Nathan McMillan <nathanmcmillan54@gmail.com>"]
edition = "2018"

[workspace]
members = [
    # Architectures
    "arch/x86/",
    # Drivers
    "drivers/nkuefi/",
    # Kernel
    "kernel/kinfo/",
    "kernel/printk/",
    # Libraries
    "lib/libcolor/",
]

[dependencies]
bootloader = "0.9.12"
register = { version = "1.0.2", optional = true }
x86 = { path = "arch/x86/" }

[features]
default = []
bios_boot = ["register"]
uefi_boot = ["register"]
