[package]
name = "stdint_uintn"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html


[dependencies]
libc = { git = "https://github.com/new-kernel/nlibc" }

[features]
no_lang_items = []
