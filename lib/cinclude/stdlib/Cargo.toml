[package]
name = "stdlib"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.131"
stdint = { path = "../stdint", features = ["no_lang_items"] }

[features]
no_lang_items = []
