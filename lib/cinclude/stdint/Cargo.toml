[package]
name = "stdint"
version = "0.1.0"
edition = "2021"

[dependencies]
_include = { path = "../_include" }
stdint_intn = { path = "../bits/stdint_intn", features = ["no_lang_items"] }
stdint_uintn = { path = "../bits/stdint_uintn", features = ["no_lang_items"] }

[features]
no_lang_items = []
