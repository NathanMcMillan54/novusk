[package]
name = "libcolor"
version = "0.1.0"
edition = "2018"
description = "A library of color types."

[dependencies]
register = { version = "1.0.2", optional = true }

[features]
default = []
vga_colors = ["register"]
uefi_colors = ["register"]
