[package]
name = "power"
version = "0.1.0"
edition = "2021"
description = "Power managment for Novusk"
license = "MIT"
publish = false

[target.'cfg(target_arch = "x86_64")'.dependencies]
x86_64 = "0.14.5"

[dependencies]
printk = { path = "../printk/" }
